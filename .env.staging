# ── Staging ────────────────────────────────────────────────────
# Render injects PORT automatically — do not hardcode it
DATABASE_URL="file:./dev.db"
CORS_ORIGIN="https://zephlo-frontend.vercel.app"
DEFAULT_TENANT_SLUG="demo"
